
// This component was previously corrupted with JSON content.
// Restoring to a basic placeholder functional component to fix build errors.
// The actual map functionality is in src/components/google-map.tsx and
// used by src/app/[lang]/contact/page.tsx.

import React from 'react';

export default function MapEmbed() {
  return (
    <div className="aspect-video w-full rounded-lg overflow-hidden shadow-lg border border-border bg-muted flex items-center justify-center">
      <p className="text-muted-foreground p-4 text-center">
        Map Placeholder. Configure Google Maps API for full functionality.
      </p>
    </div>
  );
}
